{
  "name": "heal-website",
  "version": "1.0.0",
  "private": true,
  "description": "Healthcare Education and Awareness Lab — institutional digital platform.",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=@heal/web",
    "build": "npm run build --workspace=@heal/web",
    "lint": "npm run lint --workspace=@heal/web",
    "typecheck": "npm run typecheck --workspace=@heal/web"
  },
  "engines": {
    "node": ">=24"
  },
  "dependencies": {
    "@sanity/client": "^7.26.2"
  }
}
